/**
 * This component has been removed to align with security best practices.
 * The Gemini API key must be sourced exclusively from the server-side
 * `GEMINI_API_KEY` environment variable and must not be handled through user input.
 */
const ApiKeyInputView = () => null;
export default ApiKeyInputView;
